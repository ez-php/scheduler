# APP
APP_NAME=ez-php-scheduler
APP_ENV=local
APP_DEBUG=true

# Redis — the app container reaches it as redis:6379; REDIS_HOST_PORT is the host-published port
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_HOST_PORT=6386
